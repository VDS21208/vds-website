{
  "name": "vds-website",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "extract": "python3 scripts/extract-data.py",
    "dev": "tinacms dev -c \"astro dev\"",
    "build": "npx -y @tinacms/cli@latest build || echo '[tina] build skipped or failed - continuing' ; npm run extract && astro build && node scripts/generate-sitemap.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "build:strict": "tinacms build && npm run extract && astro build && node scripts/generate-sitemap.mjs"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.2",
    "astro": "^4.16.0",
    "tailwindcss": "^3.4.13",
    "tinacms": "^2.6.0",
    "@tinacms/cli": "^1.9.0"
  },
  "devDependencies": {}
}