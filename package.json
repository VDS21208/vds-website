{
  "name": "vds-website",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "extract": "python3 scripts/extract-data.py",
    "dev": "tinacms dev -c \"astro dev\"",
    "build": "tinacms build && npm run extract && astro build && node scripts/generate-sitemap.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.2",
    "astro": "^4.16.0",
    "tailwindcss": "^3.4.13"
  },
  "devDependencies": {
    "tinacms": "^2.6.0",
    "@tinacms/cli": "^1.9.0"
  }
}